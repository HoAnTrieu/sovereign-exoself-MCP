{
  "id": "test2_debug",
  "name": "Bug detection and patch",
  "task": "Find the bug in this code and suggest a fix:\n\ndef fibonacci(n):\n    if n <= 0:\n        return []\n    fib = [0, 1]\n    for i in range(2, n):\n        fib.append(fib[i-1] + fib[i-2])\n    return fib[:n]\n\nReturn the bug explanation and the corrected code.",
  "expected_output": "bug_fix"
}
