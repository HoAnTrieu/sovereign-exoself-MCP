# Agent Instructions

Read `README.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, and `docs/HANDOFF.md` first. Run tests before modifying code. Keep MCP stdout protocol-only, preserve public tool schemas, add migrations for schema changes, never store secrets, and update tests and docs with code changes. Do not add heavy frameworks without evidence.
